@model IEnumerable<Raporter.Models.Oddzialy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazwaOddzialu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SymbolOddzialu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NazwaOddzialu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SymbolOddzialu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OddzialyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OddzialyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OddzialyID })
        </td>
    </tr>
}

</table>
