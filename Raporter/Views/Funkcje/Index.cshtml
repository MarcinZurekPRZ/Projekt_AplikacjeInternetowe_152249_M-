@model IEnumerable<Raporter.Models.Funkcje>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NazwaFunkcji)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NazwaFunkcji)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FunkcjeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FunkcjeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FunkcjeID })
        </td>
    </tr>
}

</table>
