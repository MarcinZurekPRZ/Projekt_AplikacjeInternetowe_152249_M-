@model IEnumerable<Raporter.Models.Uzytkownicy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Oddzialy.NazwaOddzialu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Imie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nazwisko)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_funkcji)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Oddzialy.NazwaOddzialu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Imie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwisko)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_funkcji)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UzytkownicyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UzytkownicyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UzytkownicyID })
        </td>
    </tr>
}

</table>
